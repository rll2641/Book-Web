services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: bookshop-app
    restart: always
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/book_shop?allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
      SPRING_DATASOURCE_USERNAME: jihoon
      SPRING_DATASOURCE_PASSWORD: 1234
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - bookshop-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      timeout: 10s
      retries: 5
      start_period: 60s

  mysql:
    image: mysql:8.0
    container_name: bookshop-mysql
    restart: always
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: book_shop
      MYSQL_USER: jihoon
      MYSQL_PASSWORD: 1234
    command: >
      --default-authentication-plugin=mysql_native_password
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --lower-case-table-names=0
      --log-bin-trust-function-creators=1
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - bookshop-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: bookshop-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - bookshop-network
    command: redis-server --appendonly yes

  flyway:
    image: flyway/flyway:9.22.3
    container_name: bookshop-flyway
    restart: "no"
    volumes:
      - ../src/main/resources/db/migration:/flyway/sql
    networks:
      - bookshop-network
    depends_on:
      mysql:
        condition: service_healthy
    command: [
      "-url=jdbc:mysql://mysql:3306/book_shop?allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul",
      "-user=jihoon",
      "-password=1234",
      "-locations=filesystem:/flyway/sql",
      "migrate"
    ]

volumes:
  mysql-data:
    driver: local
  redis-data:
    driver: local

networks:
  bookshop-network:
    driver: bridge